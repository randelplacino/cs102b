#include <stdio.h>
int main () {
      int count, limit;
      count = 0;
      limit = 3;
      while (count < limit) {
            printf("Cue the sun!\n");
            count = count + 1;
      }
      return(0);
}
